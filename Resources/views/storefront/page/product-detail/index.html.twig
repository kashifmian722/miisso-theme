{% sw_extends '@Storefront/storefront/page/product-detail/index.html.twig' %}


{% block page_product_detail_media %}

    {% if theme_config('miiso-detail-image-slider') %}
        {% sw_include '@Storefront/storefront/page/product-detail/miiso-detail-image.html.twig' with {
            'isProduct': true,
            'fallbackImageTitle': page.product.translated.name
        } %}
    {% else %}
        {{ parent() }}
    {% endif %}

{% endblock %}
